# Database-migraties

Schemawijzigingen, op volgorde van toepassing (bestandsnaam = Supabase
migration-version). Toepassen via de Supabase CLI of de MCP `apply_migration`-tool.

> De migraties van vóór 2026-06-26 (de oorspronkelijke `hive_state`, cron,
> `bezoeker_pings`, `page_404_hits`, enz.) zijn alleen in de Supabase
> migration-historie vastgelegd; vanaf 2026-06-26 staan ze ook hier in git.
